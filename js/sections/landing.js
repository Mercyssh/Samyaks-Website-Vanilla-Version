/* ============================================================
   landing.js — the landing hero is otherwise static HTML.
   The halo is now a fixed, centred CSS gradient (no mouse-follow),
   press cards + tagline are static, and the centre 3D device is
   mounted in js/three/landing-scene.js. Kept as a no-op hook so
   main.js keeps a single, predictable per-section entry point.
   ============================================================ */

export function initLanding() {}
